Optimize this work item. Improve the description, acceptance criteria, and content quality. NEVER modify the YAML frontmatter - only improve the markdown body sections.

**Current work item:**

{{content}}

Rules:
- Keep the same ## headings - do not add or remove headings
- For ## Description: make it clear, concise, and actionable. Add context if missing.
- For ## Acceptance Criteria: make each criterion specific, testable, and measurable. Remove vague items. Add missing obvious criteria.
- For ## Repro Steps (bugs): ensure steps are numbered, specific, and reproducible
- For ## System Info (bugs): ensure environment details are captured
- Use language appropriate for a {{role}} in {{context}}
- Do not change the type, title, area, iteration, state, story points, or any frontmatter field
- If story points/t-shirt seem wrong for the scope, mention it in a comment at the bottom but do not change the frontmatter

Output ONLY the complete optimized markdown file (frontmatter + body). No commentary outside the file.
